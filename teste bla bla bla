#include<stdio.h>

 int main(){
    float m, valor,v2;


    printf("entre com a quantidade de macas: ");
    scanf("%f",&m);

 if(m<12){
     
     valor=0.30*m;
     printf("o valor total da compra vai sera: %f",valor);
 }else
 
 v2=0.25*m;
 printf("o valor total da compra vai sera: %f",v2);
}
